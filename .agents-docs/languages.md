# Project Overview

PRIMARY LANGUAGES: [Go]
USAGE CONTEXT: [Go for core library/CLI in `pkg/` and `cmd/`, plus example programs in `examples/` and tests (`*_test.go`) across the repo.]

---

*This file is part of the AGENTS.md documentation system.*
