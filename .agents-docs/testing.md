# Testing Framework

TESTING FRAMEWORKS: [Go built-in testing, Testify]
TEST COMMANDS: [`make test`, `make ci-test`, `go test -v ./...`, `go test -v -race -coverprofile=coverage.out ./...`]
TEST ORGANIZATION: [Go `_test.go` files; unit tests in `pkg/**` and example tests in `examples/agent-as-tool/**`; standard Go package-scoped tests, some `mocks_test.go`]
E2E TESTING: [None detected]

---

*This file is part of the AGENTS.md documentation system.*
